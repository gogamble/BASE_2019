<tool id="simulate_reads" name="Simulate reads for Bayesian" version="0.1.0">
    <requirements>
    </requirements>
    <command detect_errors="exit_code" interpreter="python">simulate_reads.py

--input=$input
--num=$num
--prefix=$prefix
--out=$out

    </command>
    <inputs>
        <param name="input" type="data" format="tabular" label="FASTA file" help=""/>
        <param name="num" type="text" label="Read Length" help="Enter desired read length for the simulation as a whole number"/>
        <param name="prefix" type="text" label="Prefix" help="A prefix to add to the read id for distinguishing where reads came from"/>
    </inputs>
    <outputs>
        <data format="tabular" name="out" label="Simulate reads output"/>
    </outputs>
    <help><![CDATA[
        TODO: Fill in help.
    ]]></help>
</tool>

